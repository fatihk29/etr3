class Item {
  constructor(id, name, job, about, link, createdDate) {
    this.id = id;
    this.name = name;
    this.job = job;
    this.about = about;
    this.link = link;
    this.createdDate = createdDate;
  }
}

export default Item;
